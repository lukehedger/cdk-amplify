{
  "name": "cdk-amplify",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "yarn workspaces run build",
    "test": "yarn workspaces run test"
  },
  "workspaces": [
    "app",
    "infra"
  ]
}
